// import { open, login } from '../pageobjects/login.page';
// import { flashAlert } from '../pageobjects/secure.page';

describe( 'My Login application23232', () => {
    it( 'should login with valid credentials 1', () => {
        console.log( 'in it block1' );
        browser.url( 'https://www.google.co.in/' );
    } );
    it( 'should login with valid credentials 2', () => {
        console.log( 'in it block2' );
        browser.url( 'https://www.google.co.in/' );
    } );
    xit( 'should login with valid credentials 3', () => {
        console.log( 'in it block3' );
        browser.url( 'https://www.google.co.in/' );
    } );
    it( 'should login with valid credentials 4', () => {
        console.log( 'in it block4' );
        browser.url( 'https://www.google.co.in/' );
    } );
    it( 'should login with valid credentials 5', () => {
        console.log( 'in it block5' );
        browser.url( 'https://www.google.co.in/' );
    } );
} );


